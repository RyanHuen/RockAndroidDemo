apply plugin: 'groovy'
if (rootProject.release_jitpack) {
//发布 Jcenter
    apply from: 'jitpack.gradle'
} else {
//本地
    apply from: 'maven.gradle'
}

repositories {
    mavenLocal()
    jcenter()
}

dependencies {
    implementation gradleApi()
    implementation localGroovy()
}
